{
  "name": "chatbot-app",
  "version": "1.0.0",
  "description": "Simple chatbot app using a local LLM, tested with PromptFoo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "eval": "npx promptfoo@latest eval",
    "eval:view": "npx promptfoo@latest view"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
